export function AuditLog() {
  return (
    <section className="surface card">
      <h2 className="section-title">Audit log</h2>
      <p className="muted">Append-only activity history and CSV export controls.</p>
    </section>
  );
}
